import { Sidebar } from '../Sidebar/Sidebar'
type FooterType = {

}
export const Footer = (props: FooterType) => {
    return (
        <footer className='footer'>
            <Sidebar />
        </footer>
    )
}